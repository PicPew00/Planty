<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidStudioPlanty"
        tools:targetApi="31">
        <activity
            android:name=".ManageAccounts"
            android:exported="false" />
        <!-- Set Sign_In as the launcher activity -->
        <activity
            android:name=".Sign_In"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".popup_history"
            android:exported="false" />
        <activity
            android:name=".AirQuality"
            android:exported="false" />
        <activity
            android:name=".Tempeature"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false">

            <!-- Remove the MAIN action and LAUNCHER category from MainActivity -->
        </activity>
    </application>

</manifest>